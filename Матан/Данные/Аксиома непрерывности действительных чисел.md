```typst
*Действительное число* --- это элемент сножества $RR$, на котором введено 2 операции: + и $dot$, удовлетворяющие свойствам:\
1) ...\
...\
15) "Аксиома непрерывности":\
$forall A, B subset RR: A != emptyset, B!=emptyset: forall x in A, y in B: x <= y: exists space xi in RR: forall x in A; y in B: x <= xi <= y$
```