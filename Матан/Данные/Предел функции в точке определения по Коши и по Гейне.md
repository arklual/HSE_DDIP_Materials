```typst
$lim_(x->x_0) f(x) = a$
По Коши:
$forall epsilon > 0: exists delta = delta(epsilon): forall x in U_delta^o (x_0): |f(x)-a| < epsilon$\
\
По Гейне:
$forall {x_n} n in NN x_n ->_(n->oo) x_0: x_n != x_0 => f(x_n) ->_(n->oo) a$
```